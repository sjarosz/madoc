apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: sd-network
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/sd-network: "true"
  podSelector:
    matchLabels:
      io.kompose.network/sd-network: "true"
